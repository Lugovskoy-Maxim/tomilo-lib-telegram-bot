# Telegram Bot для уведомлений о новых главах

Этот бот уведомляет пользователей о появлении новых глав манги через Telegram.

## Установка

1. Клонируйте репозиторий:
   ```
   git clone <repo-url>
   cd tomilo-lib-telegram-bot
   ```

2. Установите зависимости:
   ```
   npm install
   ```

3. Создайте файл `.env` на основе `.env.example` и укажите ваш токен бота.
   - Для **продакшна** (сайт https://tomilo-lib.ru) можно не указывать `API_BASE_URL` — по умолчанию используется `https://tomilo-lib.ru/api`.
   - Для локальной разработки с mock-API укажите, например: `API_BASE_URL=http://localhost:3099/api`.
   ```
   BOT_TOKEN=ваш_токен_бота
   API_BASE_URL=https://tomilo-lib.ru/api
   ```

## Запуск

**Бот (подключается к API по `API_BASE_URL`):**
```bash
npm start
```

**Локальный API-сервер** (mock для разработки без бэкенда):
```bash
npm run server
```
Сервер поднимается на `http://localhost:3099` (порт по умолчанию 3099, чтобы не пересекаться с 3000–3003). Эндпоинты под префиксом `/api`. Чтобы бот работал с ним, в `.env` укажите:
```
API_BASE_URL=http://localhost:3099/api
```
Затем в двух терминалах: `npm run server` и `npm start`. Будут доступны каталог из 2 тайтлов, главы с тестовыми страницами и генерация PDF.

Для разработки с автоматической перезагрузкой бота:
```bash
npm run dev
```

## Решение проблем

Если вы видите предупреждение `DeprecationWarning: The 'punycode' module is deprecated`, это связано с внутренними зависимостями библиотеки telegraf. Предупреждение подавляется при запуске через `npm start`.

Если сообщения о запуске бота не отображаются сразу в консоли, это связано с буферизацией вывода. Сообщения будут отображены после завершения процесса (Ctrl+C). Это нормальное поведение Node.js в некоторых средах.

## Использование

1. Найдите вашего бота в Telegram
2. Напишите команду `/start`
3. Бот будет уведомлять вас о новых главах каждые 30 минут

## Функциональность

- Каталог тайтлов, поиск, просмотр глав и чтение в PDF
- Уведомления о новых главах, закладки («Мои тайтлы»)
- Ссылки для чтения на сайте

### Работа с API tomilo-lib.ru

По умолчанию бот обращается к `https://tomilo-lib.ru/api`. Чтобы всё работало, на сервере должны быть доступны:

| Эндпоинт | Назначение |
|----------|------------|
| `GET /titles?limit=&page=&sort=createdAt:desc` | Каталог (список тайтлов) |
| `GET /titles?search=&limit=&page=` | Поиск тайтлов |
| `GET /titles/:id` | Один тайтл (поля: name, slug, description, releaseYear, status, views, averageRanked, coverImage; опционально teletypeUrl) |
| `GET /titles/:id/chapters/count` | Количество глав (ответ: `{ count: N }` или `{ data: { count: N } }`) |
| `GET /chapters/title/:id?sort=number:asc&limit=` | Список глав (поля: _id, titleId, number или chapterNumber) |
| `GET /chapters/:id` | Одна глава с массивом `pages` (URL или пути к картинкам) |
| Один из: `GET /titles/latest-updates`, `GET /chapters/latest`, `GET /chapters?sort=createdAt:desc` | Лента «Новые главы» |

Картинки глав: в `pages` — либо полные URL (`https://...`), либо пути вида `/uploads/...` (бот допишет базовый URL сайта). Бот поддерживает разные форматы ответов (`data`, `data.data`, `data.titles` и т.д.).

### Интеграция с Teletype / Instant View

Если бэкенд возвращает для тайтла или главы поле `teletypeUrl` или `instantViewUrl`, бот покажет кнопку **«Открыть в Telegram»** — материал откроется в просмотре Telegram (Instant View) без перехода в браузер. Подойдут ссылки на [Teletype](https://journal.teletype.in) или на [Telegraph](https://telegra.ph).

### Автоматическое создание Instant View (Telegraph)

У Teletype нет публичного API для создания статей автоматически. Для автоматизации используется **Telegraph** (telegra.ph): страницы Telegraph тоже открываются в Telegram как Instant View.

**Шаг 1 — один раз получить токен Telegraph:**

```bash
npm run telegraph:account
```

В выводе будет строка вида `TELEGRAPH_ACCESS_TOKEN=...`. Добавьте её в `.env`.

**Шаг 2 — создавать страницу для главы:**

```bash
# по ID главы
TELEGRAPH_ACCESS_TOKEN=xxx npm run instant-view -- <chapterId>

# по тайтлу и индексу главы
TELEGRAPH_ACCESS_TOKEN=xxx npm run instant-view -- <titleId> <chapterIndex>
```

Скрипт запросит главу у API, соберёт страницу из картинок (`chapter.pages`) и выведет URL вида `https://telegra.ph/...`. Этот URL нужно сохранить в бэкенде в поле главы `teletypeUrl` или `instantViewUrl`.

---

**Вариант А: вызов с бэкенда по HTTP**

На сервере с этим репозиторием запустите сервис с переменными `API_BASE_URL` и `TELEGRAPH_ACCESS_TOKEN` (тот же `npm run server`). Тогда бэкенд (tomilo-lib) после сохранения главы может вызвать HTTP-запрос и сохранить полученный URL в главу.

**Эндпоинт:** `POST /api/create-instant-view`  
**Тело (JSON):** `{ "chapterId": "id_главы" }` или `{ "titleId": "id_тайтла", "chapterIndex": 0 }`  
**Ответ при успехе:** `200` и `{ "url": "https://telegra.ph/..." }`.

**Пример на бэкенде (Node.js):**

```js
const axios = require('axios');
const INSTANT_VIEW_URL = process.env.INSTANT_VIEW_SERVICE_URL || 'http://localhost:3099';

async function createInstantViewForChapter(chapterId) {
  const { data } = await axios.post(`${INSTANT_VIEW_URL}/api/create-instant-view`, { chapterId }, { timeout: 60000 });
  return data.url;
}

// После сохранения главы:
const url = await createInstantViewForChapter(savedChapter._id);
await Chapter.updateOne({ _id: savedChapter._id }, { $set: { teletypeUrl: url } });
```

Сервис Instant View нужно запускать с `API_BASE_URL=https://tomilo-lib.ru/api`, чтобы он запрашивал главу у вашего API. В бэкенде задайте `INSTANT_VIEW_SERVICE_URL` на этот хост (например `https://ваш-сервер:3099`).

---

**Создание только по первому запросу (рекомендуется):** не включайте крон — в `.env` не задавайте `ENABLE_INSTANT_VIEW_CRON` или поставьте `ENABLE_INSTANT_VIEW_CRON=false`. Тогда просмотр для главы создаётся при первом открытии этой главы в боте (и сохраняется в БД бота).

**Вариант Б: крон**

Бот может периодически находить главы без ссылки и создавать для них страницы заранее. Ничего настраивать на бэкенде не нужно, кроме поддержки обновления главы.

**В .env добавьте:**

```env
TELEGRAPH_ACCESS_TOKEN=...   # из npm run telegraph:account
ENABLE_INSTANT_VIEW_CRON=true
INSTANT_VIEW_CRON_INTERVAL_MINUTES=60   # опционально, по умолчанию 60
INSTANT_VIEW_CRON_BATCH=20              # опционально, сколько глав проверять за проход (по умолчанию 20)
```

После запуска бота (`npm start`) крон раз в указанный интервал будет:
1. Запрашивать последние главы из API (лента обновлений).
2. Для каждой главы без уже сохранённой ссылки создавать страницу на Telegraph.
3. **Сохранять ссылку в локальную БД бота** (`data/chapter-links.json`: соответствие chapterId → url). По этой ссылке бот показывает кнопку «Открыть в Telegram» при выборе главы.
4. По возможности отправлять **PATCH** на бэкенд (если реализован и не возвращает 401).

**Локальная БД:** файл `data/chapter-links.json` хранит соответствие «id главы → URL Telegraph». Бот при показе главы сначала смотрит эту БД, затем поле из API.

**Пересоздание при ошибках:**  
- В боте: задайте в `.env` `ADMIN_USER_IDS=ваш_telegram_id` (или несколько через запятую). У админа под сообщением главы появится кнопка «🔄 Пересоздать просмотр».  
- Из консоли: `npm run recreate-instant-view -- <chapterId>` или `npm run recreate-instant-view -- <titleId> <chapterIndex>` — пересоздаёт страницу Telegraph и обновляет запись в БД бота.